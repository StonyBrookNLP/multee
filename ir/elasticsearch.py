# This is a placeholder module for retrieving sentences for an Elasticsearch server.


def retrieve(question_stem, choice_text):
    # TODO: look up top K (configurable) sentences from a query like this:
    #       question_stem + " " + choice_text
    #   and require question and choice text
    #   simply return all all sentences in one list of strings
    return []
